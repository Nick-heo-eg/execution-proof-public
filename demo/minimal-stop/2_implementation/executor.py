def execute_action(user_input: str):
    """
    Simulated side effect.

    This should only be called if judgment gate returns ALLOW.
    """
    print(f"[EXECUTION] Action executed: {user_input}")
